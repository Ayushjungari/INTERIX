{
  "name": "intervo",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci --include=dev && npm run build && cd ../backend && npm ci --omit=dev",
    "start": "cd backend && npm start"
  }
}
